\n# Generate a base64-encoded 256-bit secret (Linux/macOS):
# head -c 32 /dev/urandom | base64
# On Windows (PowerShell), e.g. use OpenSSL or another tool to generate 32 random bytes and Base64-encode.

# Generate a base64-encoded 256-bit secret (Linux/macOS):
# head -c 32 /dev/urandom | base64
# On Windows (PowerShell): use OpenSSL or another tool to generate 32 random bytes and Base64-encode.

# Generate a base64-encoded 256-bit secret (Linux/macOS):
# head -c 32 /dev/urandom | base64
# On Windows: use OpenSSL or another tool to generate 32 random bytes and Base64-encode.
